[project]
name = "safety-goggle-detector"
version = "0.1.0"
description = "Real-time safety goggle detection system using face detection and YOLO"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.2",
    "opencv-python>=4.13.0.92",
    "ultralytics>=8.4.14",
]
